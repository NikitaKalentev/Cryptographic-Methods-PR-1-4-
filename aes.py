# aes.py – реализация AES-128

SBOX = [
    
]
INV_SBOX = [0] * 256
for i, v in enumerate(SBOX):
    INV_SBOX[v] = i

def gf_mul_aes(a, b):
    from utils import gf_mul
    return gf_mul(a, b, 0x11b)

def sub_bytes(state):
    return [SBOX[b] for b in state]

def inv_sub_bytes(state):
    return [INV_SBOX[b] for b in state]

def shift_rows(state):
    return [
        state[0], state[5], state[10], state[15],
        state[4], state[9], state[14], state[3],
        state[8], state[13], state[2], state[7],
        state[12], state[1], state[6], state[11]
    ]

def inv_shift_rows(state):
    return [
        state[0], state[13], state[10], state[7],
        state[4], state[1], state[14], state[11],
        state[8], state[5], state[2], state[15],
        state[12], state[9], state[6], state[3]
    ]

def mix_columns(state):
    out = [0] * 16
    for c in range(4):
        s0 = state[4*c]
        s1 = state[4*c+1]
        s2 = state[4*c+2]
        s3 = state[4*c+3]
        out[4*c]   = gf_mul_aes(s0, 2) ^ gf_mul_aes(s1, 3) ^ s2 ^ s3
        out[4*c+1] = s0 ^ gf_mul_aes(s1, 2) ^ gf_mul_aes(s2, 3) ^ s3
        out[4*c+2] = s0 ^ s1 ^ gf_mul_aes(s2, 2) ^ gf_mul_aes(s3, 3)
        out[4*c+3] = gf_mul_aes(s0, 3) ^ s1 ^ s2 ^ gf_mul_aes(s3, 2)
    return out

def inv_mix_columns(state):
    out = [0] * 16
    for c in range(4):
        s0 = state[4*c]
        s1 = state[4*c+1]
        s2 = state[4*c+2]
        s3 = state[4*c+3]
        out[4*c]   = gf_mul_aes(s0, 0x0e) ^ gf_mul_aes(s1, 0x0b) ^ gf_mul_aes(s2, 0x0d) ^ gf_mul_aes(s3, 0x09)
        out[4*c+1] = gf_mul_aes(s0, 0x09) ^ gf_mul_aes(s1, 0x0e) ^ gf_mul_aes(s2, 0x0b) ^ gf_mul_aes(s3, 0x0d)
        out[4*c+2] = gf_mul_aes(s0, 0x0d) ^ gf_mul_aes(s1, 0x09) ^ gf_mul_aes(s2, 0x0e) ^ gf_mul_aes(s3, 0x0b)
        out[4*c+3] = gf_mul_aes(s0, 0x0b) ^ gf_mul_aes(s1, 0x0d) ^ gf_mul_aes(s2, 0x09) ^ gf_mul_aes(s3, 0x0e)
    return out

def add_round_key(state, key):
    return [state[i] ^ key[i] for i in range(16)]

def key_expansion(key):
    if len(key) != 16:
        raise ValueError("Ключ AES должен быть 16 байт")
    rcon = [0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36]
    w = [0] * 44
    for i in range(4):
        w[i] = int.from_bytes(key[4*i:4*i+4], 'big')
    for i in range(4, 44):
        temp = w[i-1]
        if i % 4 == 0:
            temp = ((temp << 8) | (temp >> 24)) & 0xFFFFFFFF
            temp = (SBOX[(temp >> 24) & 0xFF] << 24) | (SBOX[(temp >> 16) & 0xFF] << 16) | \
                   (SBOX[(temp >> 8) & 0xFF] << 8) | SBOX[temp & 0xFF]
            temp ^= (rcon[(i//4)-1] << 24)
        w[i] = w[i-4] ^ temp
    round_keys = []
    for i in range(0, 44, 4):
        key_bytes = b''
        for j in range(4):
            key_bytes += w[i+j].to_bytes(4, 'big')
        round_keys.append(key_bytes)
    return round_keys

def encrypt_block(block, key):
    if len(block) != 16 or len(key) != 16:
        raise ValueError("Блок и ключ AES должны быть 16 байт")
    state = list(block)
    round_keys = key_expansion(key)
    state = add_round_key(state, round_keys[0])
    for r in range(1, 10):
        state = sub_bytes(state)
        state = shift_rows(state)
        state = mix_columns(state)
        state = add_round_key(state, round_keys[r])
    state = sub_bytes(state)
    state = shift_rows(state)
    state = add_round_key(state, round_keys[10])
    return bytes(state)

def decrypt_block(block, key):
    if len(block) != 16 or len(key) != 16:
        raise ValueError("Блок и ключ AES должны быть 16 байт")
    state = list(block)
    round_keys = key_expansion(key)
    state = add_round_key(state, round_keys[10])
    for r in range(9, 0, -1):
        state = inv_shift_rows(state)
        state = inv_sub_bytes(state)
        state = add_round_key(state, round_keys[r])
        state = inv_mix_columns(state)
    state = inv_shift_rows(state)
    state = inv_sub_bytes(state)
    state = add_round_key(state, round_keys[0])
    return bytes(state)
